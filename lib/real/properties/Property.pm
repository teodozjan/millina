# Anything that is not person
# any( dogs, cars, houses, land, even intellectual property)
#
# Logic is not the thing that we expect to have because animal
# cannot perform legal actions. Other things do not count.
#
class Property{
	has PropertyId $.id;
}
